
--copied and modified from Andrews UnitsDAEOpAliases.hs
module PDEUnitsOpAliases (($*$), ($**$), (.&&.), (.||.), (.<.), (.<=.), (.>.), (.>=.), (.==.), (.+.), (.-.), (.*.), (./.), (.**.))
where

import Pdeunits